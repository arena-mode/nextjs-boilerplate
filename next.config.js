/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Copy any other configurations from your next.config.ts here
};

module.exports = nextConfig;
