#\!/bin/sh

# Format staged files with Prettier
FILES=$(git diff --cached --name-only --diff-filter=ACMR | grep -E '\.(js|jsx|ts|tsx|json|md)$')
if [ -n "$FILES" ]; then
  echo "Formatting staged files with Prettier..."
  npx prettier --write $FILES || true
  git add $FILES
fi
